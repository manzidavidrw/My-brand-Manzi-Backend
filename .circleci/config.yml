version: 2.1

orbs:
  node: circleci/node@5.2

workflows:
  sample:
    jobs:
      - node/test:
          version: '16.10'
          pkg-manager: npm

jobs:
  node/test:
    docker:
      - image: circleci/node:16.10
    steps:
      - checkout
    timeout: 20m 
